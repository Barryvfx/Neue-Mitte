# PostgreSQL Database
DATABASE_URL="postgresql://user:password@localhost:5432/neue_mitte"

# JWT Secret (generate with: openssl rand -base64 32)
JWT_SECRET="change-this-to-a-secure-random-string"

# App URL
NEXT_PUBLIC_APP_URL="https://neue-mitte.org"
